@model WebApplication2.Models.Entity.TBLYAZAR
@{
    ViewBag.Title = "YazarGetir";
    Layout = "~/Views/Shared/Layout.cshtml";
}

<title>Yazar Güncelle</title>
<br />
<h2>Yazar Güncelle</h2>

@using (Html.BeginForm("YazarGuncelle", "Yazar", FormMethod.Post))
{<div style="margin-bottom:15px"></div>
    <div class="form-group">
        <div>
            <div style="margin-bottom:15px"></div>
            <label>Yazar ID</label>
            @Html.TextBoxFor(y => y.ID, new { @class = "form-control", @readonly = "readonly" })
        </div>
        <div>
            <div style="margin-bottom:15px"></div>
            <label>Yazar ADI</label>
            @Html.TextBoxFor(y => y.AD, new { @class = "form-control" })
        </div>
        <div>
            <div style="margin-bottom:15px"></div>
            <label>Yazar SOYADI</label>
            @Html.TextBoxFor(y => y.SOYAD, new { @class = "form-control" })
        </div>
        <div>
            <div style="margin-bottom:15px"></div>
            <label>Yazar DETAY</label>
            @Html.TextAreaFor(y => y.DETAY, new { @class = "form-control" })
        </div>
        <br />
        <button class="btn btn" :15px style="background-color: #063a60; color:white;">Yazar Bilgilerini Güncelle</button>
    </div>
}



